/**
 * Resolves the current user's Spotify access token on the server.
 * Wired to real auth when Spotify OAuth exists.
 */
export const getSpotifyAccessToken = async (): Promise<string | null> => {
  return null;
};
